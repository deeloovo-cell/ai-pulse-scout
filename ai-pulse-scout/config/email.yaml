# AI Pulse Scout — Email Configuration

email:
  # Primary recipient
  to: "deeloovo@gmail.com"

  # Sender display name and address (must match SMTP credentials)
  from_name: "AI Pulse Scout"
  from_address: ""  # Override with SMTP_USER from .env

  # Subject format — {date} is replaced with MM-DD-YYYY
  subject_template: "AI Pulse Scout -- {date}"

  # Reply-to address (optional)
  reply_to: ""
